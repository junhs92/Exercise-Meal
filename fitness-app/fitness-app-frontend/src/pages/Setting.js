function Settings() {// Should be accessible from both client and experts
    return (
      <div>
        <h1>Settings</h1>
        {/* Add settings options here, like password change, notification preferences, etc. */}
      </div>
    );
  }
  
  export default Settings;
  